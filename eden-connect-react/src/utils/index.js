import { request } from "./request";
import { setCurrentTheme, getCurrentTheme } from "./local";
import {
  getToken,
  setToken,
  removeToken,
  setUser,
  getUser,
  removeUser,
} from "./token";

export {
  setCurrentTheme,
  getCurrentTheme,
  request,
  getToken,
  setToken,
  removeToken,
  setUser,
  getUser,
  removeUser,
};
